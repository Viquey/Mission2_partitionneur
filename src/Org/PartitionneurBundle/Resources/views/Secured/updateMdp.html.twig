{% extends "::layout.html.twig" %}

{% block title parent() ~ "Choisir un autre mot de passe" %}
    
{% block content %}
    
    <center>Changement de mot de passe</center>
    {{ form(form) }}
    
    <b><br/>
        {% for flashMessage in app.session.flashbag.get('newNotOk') %}
                {{ flashMessage }}
            {% endfor %}
        {% for flashMessage in app.session.flashbag.get('oldNotOk') %}
                {{ flashMessage }}
            {% endfor %}
    </b>
            
{% endblock %}

